apiVersion: v1
kind: Service
metadata:
  name: issue-service
  labels:
    app: issue-service
spec:
  selector:
    app: issue-service
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 8098
